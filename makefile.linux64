# Programs for processing
LEX				= flex
YACC			= bison
CC				= g++
CUT				= cut

# Compiler and linker flags
CFLAGS			= -DNDEBUG -flto -march=x86-64 -std=c++14 -static -m64 -O3 -I.
LDFLAGS			= -DNDEBUG -flto -march=x86-64 -std=c++14 -static -m64 -O3 lpsolve/liblpsolve55-linux64.a -ldl \
              -static-libgcc -static-libstdc++

# Input files
FLEX_SOURCES	= $(shell find * -name "*.l")
BISON_SOURCES	= $(shell find * -name "*.y")
SOURCES			= $(shell find * -name "*.cpp" | grep -v ".\\(parser\\|lexer\\).cpp")		\
				  $(BISON_SOURCES:.y=.parser.cpp)											\
				  $(FLEX_SOURCES:.l=.lexer.cpp)
OBJECTS			= $(SOURCES:.cpp=.o)
TARGET			= verifypn-linux64

include rules.mk
