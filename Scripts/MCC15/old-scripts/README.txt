1. set up the PREFIX in BenchKit_head.sh
2. set up the PREFIX in run.sh
3. change gtimeout with timeout (for linux) in all scripts 
4. set up correctly path to VERIFYPN in classicSEQ.sh and classicMC.sh scripts
5. set up TIMEOUT in the scripts as in 4.

Finally run the run.sh script where is the directory INPUTS

BEFORE SUMISSION scripts have to be modified!!!
